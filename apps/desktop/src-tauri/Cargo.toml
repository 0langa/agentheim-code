[package]
name = "agentheim-code"
version = "1.9.0"
description = "Agentheim Code desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
